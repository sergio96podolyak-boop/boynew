#include "Generated.xcconfig"
// Optional local/CI release settings. The populated file is gitignored.
#include? "ReleaseSecrets.xcconfig"
// Supply POMARKET_ADMOB_APP_ID as an Xcode build setting.
GAD_APPLICATION_IDENTIFIER=$(POMARKET_ADMOB_APP_ID)

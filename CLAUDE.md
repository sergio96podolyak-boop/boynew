# boynew — Binance USDT-M Futures Bot

## מה זה
בוט מסחר אוטונומי ל־Binance Futures (USDT-M). שני מסלולים:
- **`AGGRESSIVE_HFT=true` (ברירת מחדל):** לולאה מהירה — סריקת יקום, דירוג הזדמנויות (מודל + פיצ’רים), `risk.evaluate`, כניסה/יציאה (`hft_open` / `hft_close`), רוטציית הון.
- **`AGGRESSIVE_HFT=false`:** מסלול קלאסי — סימבולים מ־`SYMBOLS`, OHLCV → אנליזה → אות → `assess_risk` → `execute_signal`.

## הרצה
```bash
cd boynew
./venv/bin/python main.py              # paper (ברירת מחדל מ-.env)
./venv/bin/python main.py --live       # לייב — דורש מפתחות ב-.env
./venv/bin/python main.py --no-dashboard
```
תבנית משתני סביבה: **`.env.example`** (העתק ל־`.env`, אל תעלה מפתחות לגיט).

## קבצים מרכזיים
| קובץ | תפקיד |
|------|--------|
| `main.py` | נקודת כניסה, `TradingSystem`, `_run_hft_loop` / `_run_classic_loop` |
| `config.py` | `TradingConfig` — כל הפרמטרים מ־env |
| `agents/execution.py` | הזמנות, פילטרי בורסה, `refine_sl_tp_prices`, HFT |
| `agents/risk_manager.py` | `evaluate` (HFT), `assess_risk` (קלאסי), יציאות stale, drawdown |
| `agents/scanner.py` | `discover_universe`, `scan_and_rank`, סינון מחיר מינימלי |
| `agents/binance_compat.py` | לקוח בלי `ping()` בבנייה |
| `agents/agent_monitor.py` | באס ניטור — כל סוכן מדווח פעילות (throttle, crash-safe) ל-DB עבור הדשבורד |
| `dashboard.py` | Streamlit — מרכז שליטה חי: כרטיסי סוכנים + זרם פעילות, תיק, עסקאות, גרפים |
| `dashboard_floor.py` | **רצפת המסחר** — תצוגת ה-HTML/SVG הראשית בראש הדשבורד (מודול עצמאי) |
| `dashboard_market.py` | **פאנל שוק חי** — נרות/ספר/סרט מ-Binance WebSocket ישירות לדפדפן |
| `dashboard_ledger.py` | **הפנקס** — פוזיציות, בריאות המודל, היסטוריה ויומן מערכת |
| `database/repository.py` | SQLite (כולל `agent_activity` לפיד הסוכנים) |

## פסי בטיחות (אוטומטיים — לא דורשים ניהול ידני)
בהפעלה מודפסת שורה `Safety rails (automatic) | ...` עם: paper/live, HFT, מקס פוזיציות, מקס מרג’ין לפוזיציה, **`min_notional`** (רצפת מינימום לנוטיונל ב-USDT — כדי לא לסחור ב"סנטים"), kill על drawdown יומי/סשן, stale exit, מחיר מינימלי לסימבול, סף ציון.

ב־`.env`: `HFT_MIN_NOTIONAL_USDT` (ברירת מחדל 1). בחלקים בבורסה מינימום 5 USDT — אם הזמנות נדחות, העלה ל־5.

בנוסף: `config.validate()` בודק טווחים ל־`max_margin_fraction`, גדלי טייר, וכו’.

**הגנת בורסה (SL/TP אמיתיים):** בלייב, בכל כניסה (`hft_open` / `execute_signal` / סנכרון פוזיציות בהפעלה) נשלחות הוראות `STOP_MARKET` + `TAKE_PROFIT_MARKET` עם `closePosition=true` ל-Binance (`place_protective_orders`). כך הפוזיציה מוגנת גם אם הבוט נעצר/קורס/מתנתק — מוניטור הלולאה הוא רק גיבוי. בסגירה (`_live_close_position`) ההוראות שנשארו מבוטלות (`_cancel_symbol_orders`). כשל ב-SL נרשם כ-CRITICAL בלוג.

## בדיקת מצב
`./venv/bin/python status.py` — סיכום מה-DB (ביצועים, פתוחות/סגורות, סיבות יציאה, snapshot, אירועים). `--live` מוסיף מצב חי מ-Binance: יתרה, פוזיציות, והוראות ההגנה הפעילות.

## מרכז שליטה (דשבורד)
הדשבורד (`http://localhost:8501`, עולה אוטומטית עם `main.py`) מציג בראש כרטיס לכל סוכן — System / Scanner / Model / RiskManager / Execution / Analyzer — עם נורית סטטוס (פעיל/עובד/מושהה/עצור), הפעולה האחרונה, וזמן. מתחת: זרם פעילות חי. הנתונים מגיעים מטבלת `agent_activity` שאליה הלולאה מדווחת דרך `agent_monitor`. רענון כל 4 שניות.

## רצפת המסחר (`dashboard_floor.py`)
התצוגה הראשית בראש הדשבורד — עמוד HTML אחד, סגור, שמוצג ב-iframe יחיד דרך
`components.html`. מודול עצמאי לגמרי: מקבל dict-ים מה-DB ומחזיר `(html, height)`.

**שמונה מקטעים, כולם מנתוני אמת:**
1. **סרגל טיקר** — מצב (נייר/חי), HFT, מנוע, מחזור, זמן פעילות (מ-`System/boot`), דופק אחרון, יקום, פוזיציות
2. **אריחי KPI** — הון, רווח מצטבר, רווח 24ש, אחוז הצלחה + PF, שארפ — עם ספארקליין מ-`portfolio_snapshots`
3. **צינור השלבים** — 7 שלבים (סריקה→מודיעין→ניקוד→החלטה→סיכון→ביצוע→למידה); סטטוס כל שלב נגזר מהסוכנים שבו
4. **ליבת הנחיל** — SVG מונפש: ליבה מרכזית + 8 כרטיסי סוכן (`FEATURED`) במשבצות **קבועות**, קווים חיים לליבה
5. **יומן פעילות** — זרם ממוזג של `agent_activity` + עסקאות שנסגרו (משם מגיע ה-±$ האמיתי)
6. **מסילת סוכנים** — כל 17 הסוכנים, עם היסטוגרמת פעילות אמיתית מהפיד
7. **קונצנזוס** — ממוצע `consensus` מ-`decision_audit` + אושרו/נדחו + מכפיל גודל ממוצע
8. **גרף הון** — עקומת equity + עמודות רווח/הפסד לכל עסקה

**כללי עבודה במודול:**
- **אין נתוני דמו בזרימה האמיתית.** אם המנוע כבוי, המקטעים מציגים "ממתין" ומסבירים
  איך להפעיל — אף פעם לא ממציאים מספרים. נתוני ההדגמה חיים רק ב-`_preview_data()`.
- כל מחרוזת מה-DB עוברת `esc()`. כל מספר עטוף ב-`.num` (LTR מבודד) כדי שסימן
  המינוס לא יקפוץ לסוף בתוך טקסט עברי.
- טקסט עברי ב-SVG מעוגן מימין: `text-anchor="start"` + `direction="rtl"` בקצה הימני
  של הכרטיס. עוגן משמאל גורם לטקסט לזלוג החוצה.
- **גובה ה-iframe מחושב ידנית** (`801 + rail_h + equity_h`) — Streamlit לא מודד תוכן
  בתוך רכיב HTML. הנוסחה כוילה מול מדידה בדפדפן ב-900–1700px. `scrolling=True`
  הוא רשת הביטחון לחלונות צרים. **אם משנים גבהים ב-CSS — לכיל מחדש.**
- הסוכנים ב-`FEATURED` יושבים במשבצות קבועות בכוונה: מיקום שמשתנה בכל רענון
  (כל 4 שניות) הופך את הלוח לבלתי-קריא.
- **`LIVE_WINDOW_SEC` חייב לכסות מחזור סריקה שלם.** סוכן ששתק יותר מהחלון נקרא
  "ממתין", ואם כל הסוכנים ממתינים הכותרת מכריזה **"מנוע מושבת"**. מחזור הוא
  ~1.3ש לסימבול (אימון מודל), כלומר ב-`SCAN_TOP_N=200` מחזור אורך **4.3 דקות** —
  יותר מהחלון הישן של 180ש, והמנוע נראה כבוי בזמן שהוא עובד. ברירת המחדל היא
  420ש, ו-`DASHBOARD_LIVE_WINDOW_SEC` דורס. **מעלים את `SCAN_TOP_N` — מעלים גם
  את זה.**

**בדיקה ויזואלית בלי להריץ את המערכת:**
```bash
./venv/bin/python dashboard_floor.py preview.html   # קורא מ-trading.db אם קיים
```

## פאנל השוק החי (`dashboard_market.py`)
נרות, ספר פקודות וסרט עסקאות מגיעים **ישירות מ-Binance אל הדפדפן** דרך WebSocket
ציבורי (`wss://fstream.binance.com`) — בלי מפתחות API, בלי לעבור דרך פייתון.
מעליהם מצוירים קווי הבוט: מחיר כניסה, SL, TP, ומשולשי ביצוע מעסקאות שנסגרו.

**האילוץ שמעצב את כל המודול:** הדשבורד מרונדר מחדש כל 4 שניות. Streamlit מעביר את
ה-HTML כ-prop ל-iframe — אם ה-prop משתנה, ה-iframe נטען מחדש וה-WebSocket מת.
לכן ה-HTML כאן הוא **פונקציה טהורה של מצב הפוזיציות**: אין שעון, אין "לפני X שניות",
חותמות זמן הן epoch במילישניות, מחירים מעוגלים, מפתחות ממוינים. כל עוד הבוט לא פתח
או סגר פוזיציה — ה-HTML זהה בית-בית, React לא נוגע ב-iframe, והזרם ממשיך.

> ⚠️ **כל שינוי כאן חייב לשמור על התכונה הזו.** ערך שמשתנה בכל רענון = הבהוב
> ו-WebSocket שנופל כל 4 שניות. יש בדיקה שמאמתת את זה — הריצו אותה אחרי כל שינוי:
> ```python
> a = render_market_panel(...); b = render_market_panel(...); assert a == b
> ```

- הגרף נשאר **LTR** בכוונה (זמן שמאל→ימין, ציר מחיר מימין) — ככה כל פלטפורמת
  מסחר עובדת; היפוך לטובת RTL רק מבלבל. הטקסט סביבו בעברית.
- כשל רשת (חסימה, VPN, אין אינטרנט) מציג הסבר בעברית ולא שובר את שאר הדשבורד.
- תצוגה מקדימה: `./venv/bin/python dashboard_market.py preview.html`

## מנוע ה-ML — למה הוא לא סחר, ומה תוקן
עד התיקון הזה **ה-ML לא הזיז אף עסקה מעולם**. כל העסקאות הגיעו מה-fallback
הטכני. שלושה כשלים משתלבים גרמו לזה:

**1. הציון היה בלתי-אפשרי מתמטית.** `score = probability × reliability × 100`
מול סף כניסה 72-78 דורש אמינות ≥ 0.82. אבל אמינות היא *יתרון מעל מקריות*,
ובשוק נזיל יתרון אמיתי הוא 0.05-0.30. לכן `Ranked 0 opportunities` בכל לולאה.
תוקן ב-`_edge_multiplier` — עקומה רוויה שממפה יתרון ל-0..1, כך שסף 72 נשאר
בעל משמעות: יתרון 0.20 ⇒ ציון ~74 (עובר), רעש 0.02 ⇒ ~0 (נדחה).

**2. האמינות נמדדה בדיוק כולל — המדד הלא נכון.** הבוט סוחר רק כשהמודל
מכריז כיוון בביטחון, אבל המדד שאל "כמה נרות סווגו נכון". בחלון מגמתי שבו
70% מהנרות לונג, "תמיד לנחש לונג" משיג 70% דיוק בלי יכולת — ומודל אמיתי עם
יתרון קטן *נראה גרוע ממנו*. עכשיו `_fit_eval_fold` מודד **lift מעל שכיחות
הבסיס על ההכרזות שהיינו סוחרים עליהן** בלבד. במדידה על נתונים עם יתרון ידוע
בחלון מגמתי: השיטה הישנה נתנה 0.000, החדשה 0.294 (14/14 זוהו).

**3. הבסיס להשוואה היה "אורקל".** הוא חושב מרוב מחלקות ה-*מבחן* — ידע
שאסטרטגיה אמיתית לא מחזיקה מראש, והוא נע 0.53-0.84 לפי כמה החלון היה
חד-צדדי. עכשיו: "תמיד המחלקה הנפוצה ב*אימון*", נמדד על המבחן.

**בנוסף:** חלוקת 80/20 בודדת הוחלפה ב-walk-forward של 4 חלונות (אומדן יציב
במקום מבחן של 37 שורות), נוספו משקלי מחלקה, והמודל לייצור מאומן על *כל*
הנתונים אחרי שההערכה נעשתה.

**`MIN_ML_TRAINING_CANDLES`: 240 → 480.** 240 נתנו ~186 דגימות מול 40
פיצ'רים — 4.6 דגימות לפיצ'ר, יחס שמבטיח שינון. 480 נותן ~426 (10.6 לפיצ'ר)
ו**עולה אותו דבר ב-API**: Binance מתמחר klines במדרגות, ו-100..499 הוא אותו
משקל. מעל 500 המשקל קופץ ×2.5 — לכן 480 ולא יותר.

> ⚠️ אחרי שינוי במדד או בכיול — הריצו את בדיקת ההפרדה: יעד רועש חייב לקבל
> אמינות ~0, ויעד עם יתרון מוטמע חייב לעבור 0.10. בלי זה קל להחזיר בשוגט
> מדד שנותן ציון גבוה לרעש.

## מבנה הדשבורד — שלושה רכיבים, שפה עיצובית אחת
`dashboard.py` הוא היום דק בכוונה (589 שורות במקום 2,274). כל התצוגה חיה
בשלושה מודולים שכל אחד מהם מחזיר HTML סגור ל-iframe יחיד:

1. **`dashboard_floor.py`** — רצפת המסחר: טיקר, KPI, צינור שלבים, ליבת הנחיל,
   יומן פעילות, מסילת סוכנים, קונצנזוס, גרף הון.
2. **`dashboard_market.py`** — פאנל שוק חי מ-Binance WebSocket.
3. **`dashboard_ledger.py`** — הפנקס: פוזיציות פתוחות, בריאות המודל,
   היסטוריית עסקאות, יומן מערכת.

**מה הוסר ולמה.** קודם היו מתחת לרצפה עשרה מקטעים ברכיבי Streamlit
סטנדרטיים, שנראו כמו אפליקציה אחרת שהודבקה למטה. הוסרו:
*מרכז אסטרטגיות* ו*מחסנית אוטומציה* (תצוגת הגדרות — מקומן בסיידבר),
*רדאר הזדמנויות*, *קטליזטורים*, *סנטימנט*, *מודיעין*, *TradingView*,
*ניתוח שוק* ו*איתותים אחרונים* (הסוכנים ברצפה ופאנל השוק כבר מציגים אותם),
ו*סולם האגרסיביות*. סה"כ ~1,700 שורות תצוגה וקוד מת.

> ⚠️ **אל תוסיפו מקטע תצוגה חדש ב-`st.metric`/`st.dataframe` מתחת לרכיבים
> האלה.** זה מה שיצר את הבעיה מלכתחילה. מקטע חדש נכנס לאחד משלושת
> המודולים, באותו CSS.

**רוחב מלא:** Streamlit ממרכז את התוכן גם ב-`layout="wide"`. `dashboard.py`
דורס את זה (`.block-container{max-width:100%}`) כי חדר מסחר צריך את כל המסך.

**שעון:** הטיקר מציג **שעון מקומי** עם תווית אזור הזמן, לא UTC — הבוט רץ
על המחשב של המשתמש/ת ושעון UTC נראה פשוט כשעה שגויה.

## עמלות — למה זה היה הבאג הכי יקר במערכת
עד התיקון, ה-PnL חושב כך ונשמר כך:

```python
pnl = (exit_price - entry) * qty     # LONG
pnl = (entry - exit_price) * qty     # SHORT
```

**בלי שום ניכוי עמלה**, ובטבלת `trades` לא הייתה בכלל עמודת עמלה. התוצאה:
כל תוצאה בנייר הייתה אופטימית בגובה העמלה, ובפרט **עסקאות מפסידות נרשמו
כמנצחות**. דוגמה אמיתית מהלוג: יציאת `stale_no_move` ב-TRXUSDT ב-**+0.03%**
על נוטיונל 12,334 נרשמה כרווח של +0.73 USDT — בזמן שהיא עלתה 12.33 USDT
בעמלות, כלומר **הפסד של 11.60**.

בהערכה על 12 עסקאות אמיתיות: PnL מדווח −116 מול **−259 בפועל**, ו-Profit
Factor 0.77 מול **0.57**. פי 2.2 הפרש — על המדד שקובע אם להפעיל לייב.

**התיקון:** `ExecutionAgent._roundtrip_fee()` מחשב `(מחיר כניסה + מחיר
יציאה) × כמות × עמלת_taker` — Binance גובה על הנוטיונל בכל צד בנפרד — והוא
מנוכה מה-PnL בשני מסלולי הסגירה (קלאסי ו-HFT). העמלה נשמרת גם בעמודה
`fee` חדשה ב-`trades`, עם מיגרציית `ALTER TABLE` ל-DB קיימים (כי
`CREATE TABLE IF NOT EXISTS` לא מוסיף עמודות).

> ⚠️ `estimated_taker_fee_pct` (`.env`: `ESTIMATED_TAKER_FEE_PCT`, ברירת מחדל
> 0.0005) חייב להתאים לעמלה האמיתית בחשבון. VIP0 ב-Binance Futures הוא
> 0.05% taker; עם הנחת BNB או דרגת VIP גבוהה — פחות. ערך שגוי כאן מטה שוב
> את כל הסטטיסטיקה.

**שער הכניסה מפני עמלות כבר היה קיים** ב-`risk_manager.evaluate` והוא תקין:
`min_profit_cost_ratio=3.0` (רווח צפוי לפחות פי 3 מהעמלה), `min_net_reward_risk=1.15`
ו-`min_expected_net_profit_usdt=0.25`, מול `estimated_roundtrip_cost_pct` של
0.15% (עמלה×2 + באפר החלקה). הוא מונע *כניסות* גרועות — אבל לא ידע לתקן את
*הרישום* של מה שכבר נסגר. שני הדברים נחוצים.

## זרימת פקודות — הפיצ'רים שהיו בנתונים ונזרקו
40 הפיצ'רים המקוריים נגזרים כולם ממחיר ונפח של נרות. המחקר על מיקרו-מבנה
של פרפטואלס בקריפטו מצביע דווקא על **חוסר האיזון בזרימת הפקודות** ככוח
הניבוי בטווח קצר — וזה בדיוק מה שלא היה שם.

ספר הפקודות החי לא ניתן לשחזור לאחור, ולכן אי אפשר לאמן עליו בלי לצבור
תמונות במשך שבועות. **אבל בכל נר של Binance כבר יש את המידע הזה בגרסה
מצטברת**, בשני שדות שהיו נזרקים:

| שדה בנר | מה זה |
|---|---|
| `[10]` `taker_buy_quote` | כמה מהנפח הגיע מקנייה אגרסיבית |
| `[8]` `num_trades` | כמה עסקאות הרכיבו את הנפח |

`scanner.fetch_ohlcv` שמר רק `candle[7]` (נפח בציטוט); `main.py` דווקא פירש
את כל 12 השדות בשמות נכונים — ואז זרק אותם ב-`df[[...]]` בשורה אחת.

**שישה פיצ'רים חדשים** (`FEATURE_NAMES` עלה מ-40 ל-46), כולם נגזרים משני
השדות האלה: `taker_buy_ratio` (0.5 = מאוזן), `taker_flow_delta`,
`taker_flow_ma3`, `taker_flow_z` (חריגות מול 20 נרות), `avg_trade_size_z`
(כסף גדול מול ריטייל באותו נפח), `trade_count_z` (פרץ פעילות).

מדידה על סדרה סינתטית שבה הזרימה מקדימה את המחיר בנר אחד — |מתאם| לתשואה
5 נרות קדימה, ממוצע על 6 seeds:

| פיצ'ר | אות מוטמע | רעש |
|---|---|---|
| `taker_buy_ratio` | **0.303** | 0.021 |
| `taker_flow_delta` | **0.303** | 0.021 |
| `taker_flow_z` | **0.287** | 0.013 |
| `taker_flow_ma3` | **0.157** | 0.026 |
| `avg_trade_size_z` | 0.028 | 0.019 |
| `trade_count_z` | 0.018 | 0.025 |

ארבעת פיצ'רי הזרימה מפרידים חד. השניים האחרונים לא הראו אות בסימולציה
(היא לא מייצרת התנהגות של כסף גדול) — הם זולים לחישוב ונשארו כמועמדים
לשוק אמיתי; אם `model_health` יראה שהם לא תורמים, אפשר להסיר.

> ⚠️ הפיצ'רים **מתפרקים בחן**: אם העמודות חסרות (DB או מסלול ישן),
> `taker_buy_ratio` מקבל 0.5 והשאר 0.0 — ניטרלי, לא NaN. אל תשנו את זה
> ל-NaN; `model.train` זורק שורות עם NaN וזה היה חותך את סט האימון.

## כניסה כ-maker (`MAKER_ENTRY_ENABLED`)
הבוט שלח `type="MARKET"` בשני הצדדים — כלומר **taker פעמיים**, 0.10%
הלוך-חזור. Binance גובה 0.02% על maker מול 0.05% על taker, וכניסה כ-`LIMIT`
עם `timeInForce="GTX"` (post-only) מורידה את העלות ל-**0.07%** — חיסכון של
30%. על 12 העסקאות הראשונות: 143.19 → 100.24 USDT.

**GTX ולא LIMIT רגיל:** הזמנת GTX נדחית מיד אם הייתה נסגרת כנגד הספר, ולכן
היא *מבטיחה* עמלת maker ולעולם לא נופלת בטעות ל-taker. דחייה (קוד ‎-5022‎)
היא התנהגות תקינה, לא שגיאה.

**היציאה תמיד `MARKET`.** לא נגעתי במסלול הזה ואסור לגעת בו: סטופ שלא נסגר
הוא סיכון בלתי מוגבל. השדרוג הוא בכניסה בלבד — `place_entry_order` מנתב,
ו-`_place_reduce_only_order` נשאר כפי שהיה.

**המחיר האמיתי:** הזמנת maker לא תמיד נסגרת. אם המחיר בורח — נשארים בחוץ.
לכן ברירת המחדל **כבויה**: זה שינוי התנהגות, לא תיקון באג.

> `MAKER_ENTRY_FALLBACK_MARKET=false` (מומלץ). אם הלימיט לא התמלא תוך
> `MAKER_ENTRY_TIMEOUT_SEC`, ההזמנה מבוטלת והעסקה נזנחת. רדיפה אחרי המחיר
> ב-MARKET משלמת taker **וגם** נכנסת גרוע יותר מהסטאפ שדורג — זו הדרך
> הקלאסית לשחוק אסטרטגיה. הפעילו true רק אם מדדתם שהחמצות עולות יותר.

`estimated_roundtrip_cost_pct` ב-risk_manager ו-`_roundtrip_fee` ב-execution
שניהם מתמחרים עכשיו כניסה ויציאה **בנפרד**. אם רק אחד מהם היה מתעדכן, שער
הסינון היה פוסל עסקאות שכדאיות בפועל.

## עיגון SL/TP למחיר המילוי — הבאג ששבר את יחס הסיכון בכל עסקה
`risk_manager` מאשר עסקה לפי **מרחקים באחוזים** (למשל SL 0.596%, TP 1.204%,
יחס 2.02:1, סף איזון 33%). אבל הוא מעביר אותם הלאה כ**מחירים מוחלטים**,
שנגזרו ממחיר הייחוס שהסורק דירג עליו.

מחזור סריקה מלא אורך 4-5 דקות (120 סימבולים × ~1.3ש אימון מודל). עד
שההזמנה נכנסת, המחיר זז. `refine_sl_tp_prices` **רק מעגל לגודל טיק** — הוא
לא גוזר את המרחקים מחדש. התוצאה: המחירים נשארים במקומם, והמילוי זז ביניהם.

ב-LONG שמילוי שלו יקר ב-0.33% ממחיר הייחוס:

| | סיכון | סיכוי | R:R | סף איזון |
|---|---|---|---|---|
| מה שאושר | 0.596% | 1.204% | 2.02 | 33.1% |
| מה שנשלח בפועל | 0.919% | 0.875% | **0.95** | **51.2%** |

הסטופ התרחק (מפסיד יותר) והיעד התקרב (מרוויח פחות) — **שניהם לרעתך**.
נמדד על 5 עסקאות לייב אמיתיות: החלקה 0.003%-0.326%.

**התיקון:** אחרי שמתקבל `filled_price`, שני המחירים מוכפלים ב-
`filled_price / entry_price`. זה שומר על המרחקים היחסיים בדיוק:
`sl = entry×(1-d)` ⇒ `sl×(filled/entry) = filled×(1-d)`. נוסחה אחת
שנכונה ל-LONG ול-SHORT, למילוי לטובה ולרעה. הוחל בשני מסלולי הכניסה
(`hft_open` ו-`execute_signal`), לפני `refine_sl_tp_prices`.

> ⚠️ `refine_sl_tp_prices` היא **עיגול בלבד** — לא לסמוך עליה לתיקון מרחקים.
> אם מוסיפים מסלול כניסה שלישי, העיגון חייב לקרות שם גם כן.

**מה עוד קיים ומה חסר:** `max_slippage_pct` (0.2%) רושם "strike" לסימבול
אחרי כניסה גרועה (`main.py:1257`), כך שהסורק ימנע ממנו בהמשך — אבל זה
**תגובתי**, אחרי ששולם. אין שער טריות שבודק את המחיר מחדש *לפני* השליחה.
העיגון מייתר את הצורך: הוא הופך מילוי רחוק לבלתי-מזיק ליחס הסיכון, במקום
לדחות עסקאות ולהקטין את המדגם.

## מסחר מגמה (`--level swing`) — למה סקאלפ לא עובד בחשבון קטן
עמלת סבב היא **אחוז קבוע מהנוטיונל**, ולכן היא נמדדת מול *גודל היעד*, לא מול
גודל החשבון:

| יעד | עמלת maker | חלק העמלה מהרווח |
|---|---|---|
| 0.3% (סקאלפ) | 0.07% | **23%** |
| 4.0% (מגמה) | 0.07% | **1.8%** |

זו הסיבה המבנית לעבור ל-swing בחשבון של עשרות דולרים — לא ביצועי עבר.

`--level swing` מחליף 21 הגדרות: נר 15m במקום 1m, סטופ 1.0%-3.5% במקום
0.3%-0.7%, יעד 2.5%-9.0%, יציאת `stale_no_move` **כבויה** (`STALE_EXIT_SECONDS=0`),
`MAX_RISK_PCT=0.06`, `SCORE_ENTRY=82`, ונעילת רווח שנדרכת ב-2% במקום 0.4%.

**risk parity עושה את העבודה:** `risk_manager.py:570` מגביל נוטיונל ל-
`(הון × MAX_RISK_PCT) / sl_pct`. כלומר הסיכון בדולרים קבוע ולא תלוי ברוחב
הסטופ — סימבול תנודתי מקבל סטופ רחב ופוזיציה קטנה, ורגוע מקבל סטופ צר
ופוזיציה גדולה. על 45.93$ ב-6%: סיכון 2.76$ ורווח יעד 5.51$ בכל סטופ.

### שתי מלכודות שהרמה הזאת חשפה בקוד

**1. יחס היעד/סטופ היה 1.4 קבוע — ו-TradeAnalyzer שבר אותו.** אחרי רצף מפסיד
הוא קובע `sl_mult=1.15, tp_mult=0.85`, כלומר **מרחיב סטופים ומקצר יעדים** —
מוריד את R:R בדיוק כשהוא צריך לעלות. 1.4 × 0.85/1.15 = **1.03**, סף איזון 49%.
עכשיו `TP_SL_RATIO` מחליף את הקבוע, ו-`MIN_TP_SL_RATIO` הוא **רצפה שנאכפת
אחרונה** — אחרי המכפילים ואחרי ה-clamps. אם תקרת `TP_MAX_PCT` חוסמת, הסטופ
מהודק במקום לשלוח יחס שהאסטרטגיה לא הסכימה לו.

**2. תוויות ה-ML היו מכוילות ל-1m.** `horizon=5, threshold=0.0005` היה קבוע
בקוד: 0.05% על פני 5 נרות. ב-15m זה 75 דקות, ו-0.05% הוא רעש — כמעט כל חלון
מסווג כיווני ו-FLAT נעלם מסט האימון. עכשיו `ML_LABEL_HORIZON` ו-
`ML_LABEL_THRESHOLD`; ב-swing: 4 נרות (שעה) ב-0.6%.

> ⚠️ **הסף והאופק חייבים לזוז יחד עם `HFT_TIMEFRAME`.** שינוי טווח בלי לשנות
> אותם מאמן את המודל על הגדרה של "תזוזה" שלא קיימת בטווח החדש.

## נוסחת הנוטיונל — הטעות שהשתיקה את הבוט
`risk_manager.py:541` מחלק את ההון **במספר הפוזיציות תחילה**:

```python
slice_margin = current_balance / max_pos
notional     = slice_margin * tier_pct * leverage
normal_cap   = slice_margin * leverage
margin_cap   = current_balance * max_margin_fraction * leverage
notional     = min(notional, min(margin_cap, normal_cap * size_multiplier))
```

`safe_settings.sh` הציג `equity × margin_fraction × leverage` — **בלי החלוקה
ב-`max_pos`**, ולכן דיווח יתר פי `max_pos` בכל רמה מרובת-פוזיציות. ב-`active`
(4 פוזיציות) הוא הראה 103$ במקום **25$**.

הנזק לא היה בתצוגה. `MIN_EXPECTED_NET_PROFIT_USDT` נגזר מהמספר המנופח ויצא
0.24$; על נוטיונל אמיתי של 25$ זה דורש תזוזה של **1.02%** במקום 0.30%.
הבוט עבר לרמה "פעילה" ו**הפסיק לסחור לגמרי**.

`_notional()` בסקריפט מחשב עכשיו בדיוק את מה שהמנוע ישלח, כולל שתי התקרות,
ו-`MIN_EXPECTED_NET_PROFIT_USDT` נגזר ממנו. טבלת ההשוואה מציגה את הנוטיונל
האמיתי — `max` הוא 114.83$ ולא 329$, וזה תואם את הפוזיציות שנצפו בבינאנס.

> ⚠️ **`SIZE_BASE_PCT` הוא אחוז מ*פרוסה*, לא מההון.** העלאת
> `HFT_MAX_OPEN_POSITIONS` מקטינה כל פוזיציה באותו יחס. כדי לשמור על גודל
> בעת הוספת פוזיציות צריך להעלות את `SIZE_BASE_PCT` פי אותו מספר.

## `--level unleashed` — כל שער שניתן לפתוח, פתוח
הרמה האחרונה. אחריה **לא נשאר פרמטר אחד במערכת שמגביל כניסות**.

**חמישה שערים שהיו סגורים ואף אחד מהם לא היה קשור לגודל פוזיציה:**

| שער | היה | ב-unleashed |
|---|---|---|
| `TECHNICAL_FALLBACK_SIGNALS` | `false` ב-`.env` | **`true`** |
| `TECHNICAL_FALLBACK_MIN_SCORE` | 78 | 62 |
| `BLOCK_WHEN_MODEL_UNHEALTHY` | `true` | `false` |
| `CATALYST_NEGATIVE_BLOCK` | `true` | `false` |
| `REGIME_BLOCK_*_PCT` | 1.2% | 50% (מנוטרל) |
| `MIN_ATR_PCT` | 0.001 | 0 |

**`TECHNICAL_FALLBACK_SIGNALS=false` הוא הגדול.** הוא חתך **מקור אותות שלם**:
כשציון ה-ML נמוך מהסף, `_technical_confluence` אמור לספק אות חלופי. עם
`false` הבוט סחר רק על ML — ומכיוון ש-43/110 מודלים בלבד עוברים את סף
היתרון, זה השאיר אותו בלי כלום ברוב הסימבולים.

6 פוזיציות, 20x, נר 3m, יקום 200, סף 62. על 41.96$: נוטיונל 118.89$
לפוזיציה, חשיפה 713$ (17x ההון), 1% = 7.13$ עם שש פתוחות, סיכון 12%
(5.04$) לעסקה, kill יומי ‎-25.18$‎.

**מה שנשאר ולמה:** `MIN_TP_SL_RATIO=1.5` הוא הדבר היחיד שלא נפתח. הוא לא
מגביל *כמה* עסקאות נפתחות — הוא מונע שליחת עסקה שהיעד שלה קטן מהסטופ,
כלומר עסקה שמפסידה בתוחלת עוד לפני שהשוק זז. פתיחתו לא מוסיפה פעילות,
רק הופכת כל עסקה לגרועה יותר.

> ⚠️ **מעבר לרמה הזאת אין עוד הגדרות.** כל שינוי נוסף בביצועים חייב לבוא
> מהמודל עצמו — פיצ'רים, תוויות, או אסטרטגיה — לא מכיול.

## `--level turbo` — מקסימום מינוף, פעילות וסיכון
20x, 4 פוזיציות, נר 3m, סף כניסה 70, יקום 140 סימבולים. על 45.93$:

| | |
|---|---|
| נוטיונל לפוזיציה | 195.20$ |
| חשיפה כוללת | 780.81$ = **17x ההון** |
| תזוזה של 1% | 1.95$ (×4 = 7.81$) |
| סיכון לעסקה | 10% = 4.59$ → יעד 9.19$ |
| kill יומי / סשן | ‎-18.37$‎ / ‎-32.15$‎ |
| **סטופים עד אפס** | **10** |

`SIZE_BASE_PCT=0.85` נבחר כדי שארבע הפוזיציות ייכנסו: `pre_trade_check` דורש
`margin <= available × 0.90`, ובמרג'ין של 9.76$ הרביעית נפתחת עם 16.65$ פנויים.
ב-`SIZE_BASE_PCT=1.00` הרביעית הייתה נדחית.

`MIN_PROFIT_COST_RATIO` יורד ל-2.0 (מ-3.0) ו-`SCORE_ENTRY` ל-70 — שני
השערים שקובעים כמה עסקאות עוברות.

## `--level active` — פעילות מריבוי פוזיציות, לא מכיווץ יעדים
הדרך הזולה להעלות פעילות היא **כמה פוזיציות רצות במקביל**, לא כמה קטן היעד.
העמלה נמדדת מול גודל היעד, אז 4 פוזיציות ביעד 3% עולות פחות מ-2 ביעד 0.3%:

| רמה | טווח | פוז' | סטופ | יעד | עמלה מהיעד |
|---|---|---|---|---|---|
| סקאלפ (היה) | 1m | 2 | 0.6% | 1.2% | **5.8%** |
| `active` | 5m | 4 | 0.6%-2.0% | 1.5%-5.0% | 1.7%-5.8% |
| `swing` | 15m | 2 | 1.0%-3.5% | 2.5%-9.0% | 0.8%-2.8% |

`active` על 45.93$: סיכון 1.84$ לעסקה, יעד 3.67$, סף כניסה 74 (מתחת לברירת
המחדל 78 — יותר כניסות), יקום של 100 סימבולים.

### `MAX_SAME_DIRECTION_POSITIONS` — למה זה נדרש דווקא כאן
לפני התוספת הזאת ההגבלה היחידה על פוזיציות מקבילות הייתה **ספירה**. אין
במערכת בקרת מתאם בשום מקום. אלטים בקריפטו זזים יחד, ולכן 4 לונגים שנפתחים
יחד הם **פוזיציה אחת בגודל פי 4** — והם נעצרים בסטופ יחד. בנתונים האמיתיים
זה לא תיאורטי: 17 מתוך 20 העסקאות הראשונות היו SHORT.

הבדיקה יושבת ב-`risk_manager.evaluate` ליד בדיקת ה-`Max positions` הקיימת.
`0` = בלי תקרה (ההתנהגות הקודמת); `active` קובע 3 מתוך 4, `swing` 2 מתוך 2.

> ⚠️ **כל העלאה של `HFT_MAX_OPEN_POSITIONS` חייבת לבוא עם התקרה הזאת.**
> בלעדיה מספר הפוזיציות מכפיל את גודל ההימור, לא את פיזורו.

**חשיפה:** 4 × 4% = 16% מההון אם כולן נעצרות (‎-7.35$‎ על 45.93$), מול kill
יומי של ‎-9.19$‎ — כלומר מחיקה מלאה של ארבע פוזיציות לא מפילה את המתג מיד,
אבל השנייה כן.

## כיול ורמות אגרסיביות (`safe_settings.sh`)
`.env` שכויל לחשבון גדול הורס חשבון קטן. הסקריפט מכייל את הפרמטרים המסוכנים
ומגבה קודם. `bash safe_settings.sh` מציג מה ישתנה; `--apply` מבצע. אינו נוגע
במפתחות API. `EQUITY_USDT=<הון>` משנה את בסיס החישוב (ברירת מחדל 47).

**ארבע רמות** (`--level`), כל אחת קובעת מינוף, שבר מרג'ין, גדלי טייר וספי kill:

| רמה | מינוף | מרג'ין | נוטיונל ב-47$ | 1% שווה | סטופים עד 0 |
|---|---|---|---|---|---|
| `conservative` (ברירת מחדל) | 5x | 10% | 23.50$ | 0.24$ | 133 |
| `balanced` | 10x | 15% | 70.50$ | 0.70$ | 44 |
| `aggressive` | 15x | 25% | 176.25$ | 1.76$ | 17 |
| `max` | 20x | 35% | 329.00$ | 3.29$ | 9 |

"סטופים עד 0" = כמה עסקאות מפסידות ברצף (סטופ ב-1.5% מהנוטיונל) מוחקות את
החשבון. **גודל הפוזיציה לא משפיע על תוחלת הרווח** — רק על קצב התוצאה. אם
ה-PF מעל 1 גדול מרוויח יותר; אם מתחת ל-1 גדול מפסיד מהר יותר.

ספי ה-kill עולים עם הרמה בכוונה: ב-`aggressive` סטופ אחד עולה 2.64$, ו-kill
יומי של 5% (2.35$) היה עוצר את המסחר אחרי עסקה מפסידה בודדת. לכן 15%.
**זה מרחיב את ההפסד היומי המרבי — זו העלות האמיתית של הרמה, לא רק המינוף.**

**המלכודת שהוא פותר:** `MIN_EXPECTED_NET_PROFIT_USDT` הוא שער ב**דולרים
מוחלטים**, לא באחוזים. ברירת המחדל 0.25$ נקבעה לפוזיציות גדולות; על נוטיונל
של 23.50$ היא דורשת תזוזה של **1.13%** — הבוט פשוט לא נכנס לשום עסקה,
ונראה תקוע. לכן הסקריפט גוזר אותו מהנוטיונל (`notional × 0.0023`), כך
שהתזוזה הנדרשת נשארת ~0.28% — בדיוק פי 3 מעמלת ה-maker, מה ש`MIN_PROFIT_COST_RATIO=3.0`
דורש ממילא. שני השערים מסכימים במקום להתנגש.

> ⚠️ אם משנים את גודל הפוזיציה (`LIVE_MAX_MARGIN_FRACTION`, `LEVERAGE`) —
> להריץ את הסקריפט מחדש. שער קבוע בדולרים מול נוטיונל משתנה הוא או בוט
> שלא סוחר, או בוט שסוחר על תזוזות שהעמלה בולעת.

## הערות למפתח/לעוזר AI
- **אין ערובת רווח** — זה כלי מסחר; שוק = סיכון.
- SL/TP למטבעות זולים: `refine_sl_tp_prices` + טעינת `exchangeInfo` גם ב־paper.
- הגנת SL/TP על הבורסה: `place_protective_orders` (one-way mode, `closePosition=true`).
- סימבולים מתחת ל־`MIN_SYMBOL_PRICE_USDT` (ברירת מחדל 0.05) נזרקים בסריקה.

## גרסה / מצב
קוד נבדק עם `python -m compileall` ו־`TradingConfig().validate()`. עדכון אחרון של מסמך זה: ידני — שמור סנכרון עם `config.py` כשמוסיפים משתני env.
